import React from 'react';

const ActivityBroadcast = () => {
  return (
    <div>
      <h2>活動發送</h2>
      <p>在此建立活動推播/通知給使用者。可連接後端 /api/broadcast (POST) 發送。</p>
      {/* TODO: 新增表單（標題、內容、目標族群、排程）與發送按鈕 */}
    </div>
  );
};

export default ActivityBroadcast;